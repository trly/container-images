name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  discover:
    name: Discover Images
    permissions:
      contents: read
    uses: ./.github/workflows/discover-images.yml
    with:
      is_pull_request: ${{ github.event_name == 'pull_request' }}
      base_sha: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || '' }}
      head_sha: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || '' }}

  lint:
    name: Lint (${{ matrix.image }})
    needs: discover
    if: needs.discover.outputs.images != '[]'
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        image: ${{ fromJson(needs.discover.outputs.images) }}
    uses: ./.github/workflows/lint-image.yml
    with:
      image: ${{ matrix.image }}

  build:
    name: Build (${{ matrix.image }})
    needs: lint
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        image: ${{ fromJson(needs.lint.strategy.matrix.image) }}
    uses: ./.github/workflows/build-image.yml
    with:
      image: ${{ matrix.image }}

  scan:
    name: Scan (${{ matrix.image }})
    needs: build
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        image: ${{ fromJson(needs.build.strategy.matrix.image) }}
    uses: ./.github/workflows/scan-image.yml
    with:
      image: ${{ matrix.image }}
      artifact_name: ${{ needs.build.outputs.artifact_name }}
      image_ref: ${{ needs.build.outputs.image_ref }}

  publish:
    name: Publish (${{ matrix.image }})
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [build, scan]
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        image: ${{ fromJson(needs.build.strategy.matrix.image) }}
    uses: ./.github/workflows/publish-image.yml
    with:
      image: ${{ matrix.image }}
      artifact_name: ${{ needs.build.outputs.artifact_name }}
      image_ref: ${{ needs.build.outputs.image_ref }}
    secrets: inherit
