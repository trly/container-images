# trly/caddy

A [Caddy](https://caddyserver.com/) Docker image with the [caddy-dns/porkbun](https://github.com/caddy-dns/porkbun) DNS provider module included, enabling DNS-01 ACME challenges via Porkbun.

## Build

```sh
docker build -t caddy-porkbun .
```
